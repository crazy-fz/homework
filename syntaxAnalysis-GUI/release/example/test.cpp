#include <bits/stdc++.h>
using namespace std;
int main()
{

    char c = '\n'; // 带转义char
    char ch = 'c'; // 一般char
    double f=-5.3e-5;
    int a=0xAef; //十六进制
    /* 
    带转义string
    */
    string str = "abc\n \\ 中文"; // 空格，测试压缩
    a=1
// 测试压缩功能，这样写是可以编译的
    +1;
    
    int len = str.length();
    for (int i = 0; i < len; ++i)
        cout << str[i];

    return 0;
}